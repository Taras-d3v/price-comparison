[tool.poetry]
name = "pricera.common"  # Use hyphen instead of dot
version = "0.1.0"
description = ""
authors = []
packages=[
    {include="pricera/common/*"}
]

[tool.poetry-virtualenvs]
create = false

[build-system]
requires = ["poetry>=1.0.0"]
build-backend = "poetry.core.masonry.api"
